node_modules/
.next/
out/
build/

# env & secrets
.env
.env.local
.env*.local
!.env.example

# SQLite database files
*.db
*.db-journal
prisma/dev.db*

# uploaded plaque photos (single-user local storage)
/data/uploads/*
!/data/uploads/.gitkeep

# seed cache (downloaded Open Plaques dumps + per-person JSON)
/data/cache/

# OS / editor
.DS_Store
*.log

# build artifacts
*.tsbuildinfo

# Tesseract language model — downloaded/cached at runtime (dev) or baked into
# the image at build (prod). Never belongs in git. (An earlier stray cache got
# committed once; this guard stops a repeat.)
*.traineddata

# local Claude Code settings (machine-specific)
.claude/settings.local.json
